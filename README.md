# notas

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de la tabla de multiplicar             [number] [required]
  -l, --listar   Muestra la tabla en consola
                                           [boolean] [required] [default: false]
  -h, --hasta    Límite de la tabla de multiplicar
                                               [number] [required] [default: 10]
  -c, --crear    Crea un archivo con la tabla de multiplicar
                                           [boolean] [required] [default: false]
```